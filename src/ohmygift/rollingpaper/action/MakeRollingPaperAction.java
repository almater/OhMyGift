package ohmygift.rollingpaper.action;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import ohmygift.common.Action;import ohmygift.common.ActionForward;import ohmygift.rollingpaper.db.RollingPaperDAO;public class MakeRollingPaperAction implements Action{	public ActionForward execute(HttpServletRequest request,HttpServletResponse response)	throws Exception{		// wnmook DB가져오는 코드 !		/*		MemberDAO memberdao = new MemberDAO();		ArrayList<MemberBean> memberList= new ArrayList<MemberBean>();			memberList = memberdao.getMemberList();				request.setAttribute("memberlist", memberList);*/				String my_uid = (String)request.getParameter("my_uid");		String friend_uid = (String)request.getParameter("friend_uid");		String gift_image = (String)request.getParameter("gift_image");		String gift_name = (String)request.getParameter("gift_name");		String gift_link = (String)request.getParameter("gift_link");		String gift_price = (String)request.getParameter("gift_price");		String gift_type = (String)request.getParameter("gift_type");		int gift_price_int = Integer.parseInt(gift_price);		RollingPaperDAO rollingdao = new RollingPaperDAO();		String paper_id = rollingdao.AddRollingPaper(my_uid, friend_uid, gift_image, gift_name, gift_link, gift_price_int, gift_type, null);		System.out.println(paper_id);				ActionForward forward = new ActionForward();		forward.setRedirect(true);		forward.setPath("getrolling.roll?paper_id="+paper_id);				return forward;	}}